namespace Megarender.StorageService.Models {
    public enum EnvironmentVariables {        
        PIS_HOST,
        PIS_PORT,
        SeqURL,
        PIS_HOST_EXT,
        PIS_PORT_EXT,
        DB_HOST,
        DB_PORT,
        DB_USER_FILE,
        DB_PWD_FILE
    }
}